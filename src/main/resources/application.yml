spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/photolite
    username: postgres
    password: postgres
  jpa:
    database: POSTGRESQL
    show-sql: true
    hibernate:
      ddl-auto: update
    servlet:
      multipart:
        max-file-size: 20MB
        max-request-size: 20MB